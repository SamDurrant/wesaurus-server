CREATE TABLE settings (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  dark_mode BOOLEAN NOT NULL DEFAULT FALSE,
  user_id INTEGER
      REFERENCES we_user(id) ON DELETE CASCADE NOT NULL
);